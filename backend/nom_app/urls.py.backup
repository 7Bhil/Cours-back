# Mets ici le contenu de urls.py ci-dessus
# backend/nom_app/urls.py - VERSION SIMPLE POUR DÉMARRER
from django.urls import path
from . import views

# URLpatterns DOIT être une liste
urlpatterns = [
    # Endpoint de test simple
    path('test/', views.test_api, name='test-api'),
    
    # Endpoint langages
    path('languages/', views.get_languages, name='languages-list'),
    
    # Progression
    path('progress/', views.LanguageProgressAPIView.as_view(), name='progress'),
]

# Si tu veux garder tes ViewSets, fais-le plus tard